function inventory(data){
   let res = [];

   for(let line of data){
        let [name, level, items] = line.split("/");
        let heroes = {
            name
        }
   }
} 

inventory();