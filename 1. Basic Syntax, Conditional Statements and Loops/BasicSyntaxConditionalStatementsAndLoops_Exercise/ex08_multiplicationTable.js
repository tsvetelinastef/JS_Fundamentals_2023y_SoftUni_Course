function multiplicationTable(n){
    for (let i = n; i <= n; i++) {
       for (let k = 1; k <= 10; k++) {
        let  multiplication =  i * k;
        console.log(`${i} X ${k} = ${multiplication}`);
       }
        
    }
}

//multiplicationTable(5);
multiplicationTable(2);
